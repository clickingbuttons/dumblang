fn main() u8 {
	const a =
		\\Hello world!
		\\No ending spaces or tabs allowed at end of multiline string literals
		\\because it's hard to read. Newlines are fine.
		\\
	;
	const b = "asdf";
	const c = 'g';
	return a.len + b.len + c;
}
