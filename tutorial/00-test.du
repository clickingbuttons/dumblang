// Test blocks are labeled function bodies. You MUST include a label. They are
// useful for selecting tests and good documentation.

test "simplest assertion" assert 1

test "nested blocks" {
	test "inner block 1" assert 1
	test "inner block 2" assert 2
}
